
// 开启内联
{$define FPDEV_INLINE}

// windows下开启超长路径支持
{$define FPDEV_LONG_PATH}

// 使用 fpc提供的exepath(paramstr(0))
{$define FPDEV_FPC_EXEPATH}

// 使用 fpc提供的cwd(GetCurrentDir)
{.$define FPDEV_FPC_CWD}
